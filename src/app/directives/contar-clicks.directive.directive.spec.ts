import { ContarClicks.DirectiveDirective } from './contar-clicks.directive.directive';

describe('ContarClicks.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new ContarClicks.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
