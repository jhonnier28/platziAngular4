import { Resaltar.DirectiveDirective } from './resaltar.directive.directive';

describe('Resaltar.DirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new Resaltar.DirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
